import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Sanity check - app initializes', () {
    // The full app requires Google services to be configured.
    // This test verifies the test environment works.
    expect(1, 1);
  });
}